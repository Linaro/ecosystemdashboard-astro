---
import { Image } from "astro:assets";
import type { CollectionEntry } from "astro:content";
// import logo from `${post.data.project.logo}`;

const { post } = Astro.props;
const logoUrl = post.data.project.logo;
---

<div class="flex flex-col items-center gap-2">
  <a href={`projects/${post.slug}`}>
    <div class="rounded-full h-48 w-48 bg-white">
      <!-- {logoUrl ? <Image src={post.data.project.logo}/> : <h1>no logo</h1>} -->
    </div>
    <p class="text-xl mt-0">{post.data.project.title}</p>
  </a>
</div>
